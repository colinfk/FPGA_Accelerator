# 第 1 次学习：参数化流水线 MAC 与自检验证

以下内容可以直接复制到 ChatGPT：

```text
你是一名有 FPGA RTL、Vivado 和 AI Accelerator 工程经验的导师。请以“分段教学”的方式带我完成一次“参数化流水线 MAC + 自检 Testbench”的学习与实践。

我的背景：我已经掌握 Verilog 基本语法、组合逻辑、时序逻辑、寄存器、简单 FSM，并了解 FIFO、BRAM、PE、MAC、Systolic Array 和 DMA 的基本概念，但还没有稳定完成“规格 → RTL → Testbench → 仿真 → 综合 → 资源/时序分析”的闭环。

完整学习目标（不要一次讲完）：
1. 理解 MAC 的输入输出、signed arithmetic、位宽增长、累加器位宽和溢出风险。
2. 设计一个参数化 MAC，至少支持 DATA_W 和 ACC_W 参数。
3. 比较无流水线和 1 级/2 级流水线在 latency、throughput、时序和资源上的差异。
4. 为模块设计 ready/valid 或清晰的 valid/latency 接口，并说明握手语义。
5. 编写 self-checking Testbench，自动比较 DUT 与参考模型的结果，不要只依赖波形观察。

本次只讲第 1 小节：MAC 的规格、signed arithmetic 和位宽增长。

严格遵守以下节奏：
- 第一条回复只允许包含：本次完整学习的 3–6 小节目录；第 1 小节的解释；一个小位宽手算例子；一个检查问题。
- 不要在第一条回复中给出完整 MAC RTL、完整 Testbench、流水线比较、Vivado 综合分析或最终总结。
- 第 1 小节结束后必须暂停，等待我用自己的话回答检查问题或说“继续”。
- 我回答后，再只讲下一小节；每次只讲一个小知识点或一个小任务。
- 如果我说“没理解”，请换一种更直白的方式解释同一小节，不要跳到下一小节。

第 1 小节必须讲清楚：
- 有符号数和无符号数在 Verilog/SystemVerilog 中的区别；
- DATA_W 位乘法结果为什么通常需要 2×DATA_W 位；
- 累加器为什么需要比乘积更宽；
- 截断、溢出和饱和的区别；
- 用 4 位或 5 位数字完成一个手算例子，并明确每一步的位宽。

约束：
- 不要直接跳到复杂 Systolic Array；
- 不要把“代码能编译”当成“设计正确”；
- 所有不确定的 Vivado/器件相关结论要明确标注为推测，并告诉我如何验证；
- 每一部分结束后给我一个简短问题或小任务，等我回答后再继续；
- 当前回复只需要一个检查问题；后续等我完成每一小节后再给对应的小任务。不要一次输出过多内容。
```

使用后，把 ChatGPT 的回答、你的回答、代码或仿真结果发给我，我会据此生成：

`01_RTL基础与验证/学习记录/001-参数化流水线MAC与自检验证.md`
